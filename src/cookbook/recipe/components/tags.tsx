/** @jsx jsx */
import { jsx } from "@emotion/core";

export default function Tags() {
  return (
    <div
      css={{
        flex: "0 0 40%",
        display: "flex",
        flexFlow: "row wrap",
        justifyContent: "flex-end"
      }}
    ></div>
  );
}
